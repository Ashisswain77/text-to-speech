# Backend API Base URL
# For Vercel production deployment with /api rewrite proxy, keep this EMPTY ("")
# so all requests use relative /api paths and remain same-origin for Strict cookies.
# In local development, keeping this empty also works with Vite's dev proxy (localhost:5000).
VITE_API_BASE_URL=
